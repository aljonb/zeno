# ============================================
# RSS Bot System Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# Command: cp env.example .env

# ============================================
# Supabase Configuration
# ============================================
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Your Supabase project URL
SUPABASE_URL=https://your-project-id.supabase.co

# IMPORTANT: Use the SERVICE ROLE key (not anon key)
# The service role key bypasses Row Level Security and is needed for bot operations
# Keep this secret and never commit to version control
SUPABASE_SERVICE_KEY=your-service-role-key-here

# ============================================
# OpenAI Configuration
# ============================================
# Get your API key from: https://platform.openai.com/api-keys

OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Override the default model (default: gpt-4o-mini)
# Options: gpt-4o-mini, gpt-3.5-turbo, gpt-4
# OPENAI_MODEL=gpt-4o-mini

# ============================================
# Bot Configuration
# ============================================

# Timezone for logging (default: UTC)
# TZ=UTC

# Log level (default: info)
# Options: error, warn, info, debug
# LOG_LEVEL=info

# Maximum concurrent RSS feed fetches per bot run (default: 3)
# MAX_CONCURRENT_FEEDS=3

# Time window to fetch items (in minutes, default: 60)
# Only process RSS items published within this many minutes
# FETCH_WINDOW_MINUTES=60

# Maximum posts per bot per run (default: unlimited)
# Set to a specific number if you want to limit total posts per run
# MAX_POSTS_PER_RUN=999999

# Maximum posts per individual RSS feed (default: unlimited)
# Set to a specific number if you want to limit posts from each feed
# MAX_POSTS_PER_FEED=999999

# Process each RSS feed separately (default: true)
# When true: Takes top N items from EACH feed for balanced representation
# When false: Combines all feeds and takes top N overall (one busy feed could dominate)
# PROCESS_FEEDS_SEPARATELY=true

# Dry run mode - don't actually create posts (default: false)
# Useful for testing. Set to 'true' to enable.
# DRY_RUN=false

# ============================================
# Rate Limiting
# ============================================

# Delay between OpenAI API calls in milliseconds (default: 1000)
# Prevents rate limiting on OpenAI API
# OPENAI_DELAY_MS=1000

# Retry attempts for failed API calls (default: 3)
# RETRY_ATTEMPTS=3

# ============================================
# Notes
# ============================================
# 1. Never commit the .env file to version control
# 2. Use the Supabase SERVICE ROLE key, not the anon key
# 3. Keep your OpenAI API key secure
# 4. Test with DRY_RUN=true first before going live

